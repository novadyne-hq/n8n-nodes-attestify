// n8n loads nodes/credentials via the `n8n` field in package.json (pointing at the
// compiled files in dist/). This entrypoint exists only to satisfy `main`; it carries
// no runtime logic.
export {};
